export interface Theme {
  icons: {
    color: string
    hover: string
  }
  texts: {
    color: string
    hover: string
  }
  backgrounds: {
    color: string
    hover: string
    menu: string
    toolbars: string
    modals: string
  }
  inputs: {
    color: string
    back: string
  }
}
